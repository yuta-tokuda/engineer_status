name: RuboCop

on:
  pull_request:
    paths:
      - '**'

jobs:
  rubocop:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build Docker image
        run: |
          docker compose build engineer_status
      
      - name: Run Rubocop
        run: docker compose run engineer_status bundle exec rubocop
